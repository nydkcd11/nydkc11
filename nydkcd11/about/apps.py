#App data for the About App
#Chances are you won't ever need to touch apps.py files (besides initial setup)
from django.apps import AppConfig

#holds the internal name of the app (for Django usage)
class AboutConfig(AppConfig):
    name = 'about'
